# Intermediate training output. nanomodel.js is the packed model that ships,
# and it is committed; model.json is just the step before packing.
nanotech/train/model.json
node_modules/
__pycache__/
*.pyc
