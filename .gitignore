# Generated build artifact: index.html with three.js inlined so the game
# runs with no internet. Rebuild it from index.html rather than editing it,
# or it will drift out of sync with the real source.
dreamvision-offline.html
